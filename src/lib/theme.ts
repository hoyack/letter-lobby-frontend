// src/lib/theme.ts
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#007bff', // match bootstrap primary or choose your own
    },
  },
});

export default theme;
